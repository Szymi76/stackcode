/api/auth 
	@POST /login (email, password)
	@POST /register (displayName, email, password)
	@GET /google 
	@GET /github
	@PATCH /update-display-name (displayName)
	@PATCH /update-profile-photo (file in form data)
	@PATCH /update-password (oldPassword, newPassword)
	@PATCH /verify-email
	@PATCH /reset-password (email)
	@DELETE /delete-account 
	@GET /logout
	@GET /refresh

/api/question
	@POST /add (title, content, tags)
	@GET /get-by-title (title from params)
	@POST /add-comment (answerID, content)
	@POST /add-answer (questionID, content)
	@POST /vote (questionID, type)
	@POST /toggle-marked (questionID)
	@PATCH / (title, content, tags)
	@DELETE / (questionID)

/api/questions
	@GET /marked-by-user
	@GET /all-by-user

/api/report
	@POST /question (questionID),
	@POST /answer (answerID)


	
	