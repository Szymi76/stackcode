/api/auth 
	@POST /login (email, password) ✔️
	@POST /register (displayName, email, password) ✔️
	@GET /google ✔️
	@GET /github 
	@PATCH /update-display-name (displayName) ✔️
	@PATCH /update-profile-photo (file in form data)
	@PATCH /update-password (oldPassword, newPassword)
	@PATCH /verify-email
	@PATCH /reset-password (email)
	@DELETE /delete-account 
	@GET /logout ✔️
	@GET /refresh ✔️

/api/question
	@POST /add (title, content, tags) ✔️
	@GET /by-title/:title ✔️
	@PATCH /toggle-vote (questionID, type) ✔️
	@PATCH /toggle-marked (questionID) ✔️
	@PATCH /edit (questionID, title, content, tags) ✔️
	@DELETE /delete (questionID) ✔️

/api/questions
	@GET /marked-by-user ✔️
	@GET /created-by-user ✔️

/api/comment
	@POST /add (answerID, content) ✔️

/api/answer 
	@POST /add (questionID, content) ✔️
	@PATCH /edit (answerID, content) ✔️
	@PATCH /toggle-vote (answerID, type) ✔️
	@DELETE /delete (answerID) ✔️

/api/report
	@POST /question (questionID),
	@POST /answer (answerID)


	
	